if(CONFIG_ZMK_DISPLAY AND CONFIG_ZMK_DISPLAY_STATUS_SCREEN_CUSTOM AND ((NOT CONFIG_ZMK_SPLIT) OR CONFIG_ZMK_SPLIT_ROLE_CENTRAL))
    set(DONGLE_DISPLAY_DIR ${ZEPHYR_ZMK_DONGLE_DISPLAY_MODULE_DIR}/boards/shields/dongle_display)

    zephyr_library()
    zephyr_library_sources(${ZEPHYR_BASE}/misc/empty_file.c)
    zephyr_library_include_directories(${ZEPHYR_LVGL_MODULE_DIR})
    zephyr_library_include_directories(${ZEPHYR_BASE}/lib/gui/lvgl/)
    zephyr_library_include_directories(${ZEPHYR_BASE}/drivers)
    zephyr_library_include_directories(${CMAKE_SOURCE_DIR}/include)
    zephyr_library_include_directories(${DONGLE_DISPLAY_DIR})

    zephyr_library_sources(custom_status_screen.c)
    zephyr_library_sources(${DONGLE_DISPLAY_DIR}/widgets/modifiers.c)
    zephyr_library_sources(${DONGLE_DISPLAY_DIR}/widgets/modifiers_sym.c)
    if(CONFIG_ZMK_HID_INDICATORS)
        zephyr_library_sources(${DONGLE_DISPLAY_DIR}/widgets/hid_indicators.c)
    endif()
    zephyr_library_sources(${DONGLE_DISPLAY_DIR}/widgets/layer_status.c)
    if(CONFIG_ZMK_BATTERY)
        zephyr_library_sources(${DONGLE_DISPLAY_DIR}/widgets/battery_status.c)
    endif()
endif()
